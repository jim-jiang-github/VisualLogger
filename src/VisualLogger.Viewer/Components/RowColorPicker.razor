@foreach (var rowColor in RowColors)
{
    <MChip Small Class="ma-1" Style="padding: 0 6px !important"
       Color=@(rowColor.BackgroundColor)
       TextColor=@(rowColor.ForeColor)>
        Aa
    </MChip>
}